/*
const button = document.querySelector("#show-pass");
button.addEventListener("click", function() {
    const buttonInput = document.getElementById("account_password");
    const type = buttonInput.getAttribute("type");
    if (type == "password") {
        buttonInput.setAttribute("type", "text");
        button.innerHTML = "Hide Password";
    } else {
        buttonInput.setAttribute("type", "password");
        button.innerHTML = "Show Password";
    }
});
*/